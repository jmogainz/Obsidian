[[Helpers]]
```bash
# Copy .so files to site-packages directory of the conda environment

conda_site_packages=$(python -c "import site; print(site.getsitepackages()[0])")

echo "Copying .so files to $conda_site_packages"

cp ../install/lib/*Wrapper*.so $conda_site_packages

chmod 775 $conda_site_packages/*Wrapper*.so

```
